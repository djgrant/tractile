Tractile($('.tractile--fullscreen'), {
  interval: 4000
});

// Tractile($('.tractile'), {
//   interval: 4000
// });

Tractile(document.getElementById('carousel'), {
  interval: 3000,
  progressContainer: document.getElementById('carousel-progress')
});

// Tractile(document.querySelectorAll('.tractile'), {
//   interval: 5000
// });

// Tractile(document.querySelector('.tractile'));